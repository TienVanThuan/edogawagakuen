let mySwiper = new Swiper(".swiper-container", {
    // Optional parameters
    loop: true,
    effect: "fade",
    autoplay: {
        delay: 4000,
        disableOnInteraction: false
    },
    speed: 2000,
});
